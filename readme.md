# GSFC Satellite Orbit

WIP prototype for a visualized representation of GSFC satellites currently in orbit.

### Running the project

-   `npm install`
-   `cp .env.example .env`
-   insert your api key in `.env`
-   `npm run start`
